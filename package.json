{
  "name": "slide-remote-control",
  "version": "1.0.0",
  "private": true,
  "description": "Slide deck + remote control, synced over WebSocket, deployable as a single Render web service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  }
}
